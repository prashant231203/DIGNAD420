beta beta_t,
            delta_x delta_n delta_zi delta_za,
            alpha_k alpha_z alpha_x alpha_n,
            a_k a_z a_no a_xo,
            psi_x psi_n,
            xi_x xi_n,
            sigmah phhi kappah,
            nu_x nu_n,
            sigma_x sigma_n,
            gama_n gama_x gama_m rho_x rho_m,
            tau omega g epsilon eta phi miu s_o s_s rstar nu etag nug a_ratio VA_n,
            share_bstar share_b share_d,
            lambda lambda_h lambda_hl,
            lambda1 lambda2 lambda3 lambda4 lambda5 lambda6,
            yo ho fo ro To do bo P_zo r_dco dco eo eho grantso oilro remito  q_xo q_no bstaro,
            upsilon Lo hlo wo a_za
            b_x b_n R_zio R_zao zio zao zieo zaeo Savo,
            ppi_nd_x ppi_nd_n pnu_nd pvarpi_z pvarpi_kx pvarpi_kn,
            b_nx prho_z pxi_z pgamma_s pgamma_ax pgamma_an